const name = 'Shaun';

// Functions

const greet = () => 'hello';

let resultOne = greet();
console.log(resultOne);

// Methods (are function that are associated with an an object/data-type)
// are called using dot notation

let resultTwo = name.toUpperCase();
console.log(resultTwo);
